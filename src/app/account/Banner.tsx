import React from 'react';

const Banner = () => {
  const BannerStyle = {
    // width: '90rem',
    height: '12.5rem',
    background: '#D9D9D9',
  };

  return (
    <div style={BannerStyle}>
    </div>
  );
};

export default Banner;
